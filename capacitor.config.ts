import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hasan.app.tamildua',
  appName: 'முஸ்லிம்களின் அன்றாடப் பிரார்த்தனைகள்',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
